def generate_path(user_id: str, profile: dict) -> dict:
    """Generate a full learning path (milestones + steps) for the user."""
    return None


def generate_explanation(profile: dict, course: dict) -> str:
    """Return a 2-sentence explanation for why this course fits the learner."""
    return None


def get_path(path_id: str, user_id: str) -> dict:
    """Fetch a path with steps + course details from Supabase (ownership checked)."""
    return None
