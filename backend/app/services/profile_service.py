def extract_profile(goal_text: str) -> dict:
    """Extract a structured profile from free-text goal via Groq LLM."""
    return None


def upsert_profile(user_id: str, data: dict) -> dict:
    """Upsert profile row for the given user into Supabase."""
    return None
