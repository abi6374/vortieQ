import React from 'react'
import AccountPage from './AccountPage'

/**
 * SettingsPage — redirects/renders the unified Account & Settings page.
 */
export default function SettingsPage() {
  return <AccountPage />
}
